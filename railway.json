{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c 'PORT=${PORT:-18789} && mkdir -p /tmp/.openclaw && export OPENCLAW_DATA_DIR=/tmp/.openclaw && npx openclaw gateway --bind lan --port $PORT --auth token --token $OPENCLAW_GATEWAY_TOKEN --allow-unconfigured'",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
