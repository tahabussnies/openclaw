{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c \"mkdir -p /data/.openclaw /data/workspace && export OPENCLAW_STATE_DIR=/data/.openclaw && export OPENCLAW_WORKSPACE_DIR=/data/workspace && exec npx openclaw gateway --bind lan --port ${PORT:-8080} --auth token --token \\\"$OPENCLAW_GATEWAY_TOKEN\\\" --allow-unconfigured\"",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
